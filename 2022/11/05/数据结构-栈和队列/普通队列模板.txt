#include<iostream>
using namespace std;
const int N = 100010;

// hh 表示队头，tt 表示队尾，初始时队列为空，hh为0，tt为-1
int q[N], hh = 0, tt = -1;

// 向队尾插入一个数
// 队列中第一个元素存于q[0]还是q[1]全凭个人喜好
q[ ++ tt] = x; // 此时队列中第一个元素存于q[0]，队列中第二个元素存于q[1]，以此类推……

// 从队头弹出一个数
hh ++ ;

// 取队头的值
q[hh];

// 其实用数组模拟队列时，还能取队尾的值
// q[tt];

// 判断队列是否为空
if (hh <= tt)
{

}